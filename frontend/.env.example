# URL of the deployed backend API.
# In development this defaults to http://localhost:5000 if not set.
# In production set this to your Render (or other) backend URL.
VITE_API_URL=https://your-backend.onrender.com
